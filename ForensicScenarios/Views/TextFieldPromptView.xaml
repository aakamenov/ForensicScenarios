<Window x:Class="ForensicScenarios.Views.TextFieldPromptView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ForensicScenarios.Views"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="180" Width="280">
    
    <Grid>
        <StackPanel Margin="20 20 20 20">
            <Label Margin="0 10 0 0" Content="{Binding Label}"/>
            <TextBox Margin="0 10 0 0" Text="{Binding TextBoxContents, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Margin="0 10 0 0" Content="{Binding ButtonText}" cal:Message.Attach="Submit"/>
        </StackPanel>
    </Grid>
</Window>
